```sh
kubectl run debug --image=busybox --dry-run=client -o yaml > debug.yaml
```

这条命令的作用是：**生成一个用于创建 busybox pod 的 YAML 配置文件，而不实际在集群中运行它。**

简单来说，这是一个“只开方不抓药”的操作，目的是为了快速获得一个标准的资源定义文件。

--dry-run 通常与 -o 成对出现，如果不指定 `-o yaml` 或 `-o json`，这条命令就像是“对着空气打了一拳”，没什么意义。
