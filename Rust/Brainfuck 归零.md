> [!TIP]
> 设置当前单元格的值为 255，最简单的办法就是：[-]（归零），然后 -（减一）。

\[-] 是对单元格执行循环减 1 操作的操作，直到单元格的值为 0 时退出循环。

接着执行一个 `-`（减一）：

- 在 Rust 实现中，如果你使用了 `wrapping_sub(1)`：
  - `0u8.wrapping_sub(1)` 会发生下溢出（Underflow），直接回绕到该类型的最大值：**255**。
- 在 C 语言实现中：
  - `unsigned char` 的 `0 - 1` 也会得到 `255`。


