**kind = Kubernetes IN Docker**  
它是一款用 Docker / Podman 容器，在本机快速创建 Kubernetes 集群的工具

### kind 最适合用来干什么？

✅ 学习 Kubernetes  
✅ 验证 YAML / Helm  
✅ 模拟多集群  
✅ CI 中跑集群测试

🚫 不用于生产环境

### 创建特定集群

使用 --name 选项指定集群名称

```sh
~ > kind get clusters
enabling experimental podman provider
No kind clusters found.
~ >

# 创建一个名为 dev-cluster 的集群
~ > kind create cluster --name dev-cluster
enabling experimental podman provider
Creating cluster "dev-cluster" ...
 ✓ Ensuring node image (kindest/node:v1.35.0) 🖼
 ✓ Preparing nodes 📦
 ✓ Writing configuration 📜
 ✓ Starting control-plane 🕹️
 ✓ Installing CNI 🔌
 ✓ Installing StorageClass 💾
Set kubectl context to "kind-dev-cluster"
You can now use your cluster with:

kubectl cluster-info --context kind-dev-cluster

Not sure what to do next? 😅  Check out https://kind.sigs.k8s.io/docs/user/quick-start/
~ >
~ > kind get clusters
enabling experimental podman provider
dev-cluster
~ >
```

### 关于 `enabling experimental podman provider`

kind 正在使用 **Podman Provider（实验性）**

原因：

- 机器上没有安装 Docker
- 或 kind 检测到 Podman

### 删除特定集群

也要用 --name 指定要删除的某个集群

```sh
~ > kind delete cluster --name dev-cluster
enabling experimental podman provider
Deleting cluster "dev-cluster" ...
Deleted nodes: ["dev-cluster-control-plane"]
~ >
~ > kind get clusters
enabling experimental podman provider
No kind clusters found.
```

### 操作默认集群

不指定 --name 时操作的是默认集群，其实就相当于在操作一个名为 kind 的集群

```sh
# 不指定集群名称
~ > kind create cluster
enabling experimental podman provider
Creating cluster "kind" ...
 ✓ Ensuring node image (kindest/node:v1.35.0) 🖼
 ✓ Preparing nodes 📦
 ✓ Writing configuration 📜
 ✓ Starting control-plane 🕹️
 ✓ Installing CNI 🔌
 ✓ Installing StorageClass 💾
Set kubectl context to "kind-kind"
You can now use your cluster with:

kubectl cluster-info --context kind-kind

Thanks for using kind! 😊
~ >

# 同名集群无法被重复创建
~ > kind create cluster
enabling experimental podman provider
ERROR: failed to create cluster: node(s) already exist for a cluster with the name "kind"
~ >

# 无法创建 --name kind 集群，因为默认集群的名字其实就是 kind
~ > kind create cluster --name kind
enabling experimental podman provider
ERROR: failed to create cluster: node(s) already exist for a cluster with the name "kind"
~ >

# 集群的删除命令可以重复执行，删除一个不存在的集群时不会报错
~ > kind delete cluster --name kind
enabling experimental podman provider
Deleting cluster "kind" ...
Deleted nodes: ["kind-control-plane"]
~ >
~ > kind delete cluster
enabling experimental podman provider
Deleting cluster "kind" ...
~ >
```
